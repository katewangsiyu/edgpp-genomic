% Per-chrom / per-trait / per-cross-dataset tables live here.
% Source outputs: outputs/{aggregator, selective, trait_loo, cross_dataset}

\TODO{Port per-partition CSVs as LaTeX tables:
\begin{itemize}
    \item Per-chrom AUPRC + coverage (22 chroms $\times$ 5 metrics)
    \item Per-OMIM metrics (30 traits Mendelian)
    \item Per-complex-trait metrics (28 traits)
    \item Cross-dataset full grid (2 directions $\times$ 2 feature sets)
    \item $\shat$-bin breakdown K=5 per regime
\end{itemize}
All tables generated via pandas \texttt{to\_latex()} with booktabs; numbers in outputs/\*/\*.csv.}
