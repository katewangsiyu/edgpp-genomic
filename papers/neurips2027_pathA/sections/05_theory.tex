% Compressed for NeurIPS 2026. All proofs + auxiliary lemmas (T1, T2, T1', T2', T3-loc, T3.b, T4) in App.~\ref{app:proofs}.

This section states HCCP's four main-text theorems. Auxiliary lemmas (T1/T2 corollaries; T1$'$/T2$'$/T4 robust counterparts via \citet{barber2023exchangeability} Thm.~2; T3-loc feature-ball extension; T3.b $\shat$-perturbation robustness) are stated and proved in App.~\ref{app:proofs}.

\subsection{Bin-conditional coverage (T3 / T3$'$)}
\label{sec:theory:t3prime}

\begin{theorem}[T3 bin-conditional coverage, exact]
\label{thm:t3}
Under \ref{ass:A1prime} and A2-cell, for each cell with $n_{k,b} \geq \lceil 1/\alpha \rceil$ and continuous $F_{kb}$,
$1 - \alpha \leq P\bigl(Y \in \pset(X) \mid Y = k,\, b(X) = b\bigr) \leq 1 - \alpha + 1 / (n_{k,b} + 1)$.
\end{theorem}

\noindent\textbf{Positioning vs.\ \citet{barber2020limits}.} We do not claim pointwise conditional coverage $P(Y \in \pset(X) \mid X = x) \geq 1 - \alpha$, ruled out in finite samples without structural assumptions. T3 conditions on the $(y, b)$-partition event; the $\shat$-bins are the coarsest practically useful strengthening of standard class-conditional CP that still carries finite-sample bounds.

\begin{theorem}[T3$'$ bin-conditional coverage, robust]
\label{thm:t3prime_main}
Let $\bar\delta_{\mathrm{TV}}^{(k,b)}$ be the average TV distance between calibration-fold and test-fold conditional score distributions in cell $(k, b)$. Under \ref{ass:A1prime} alone (without A2-cell), for each cell with $n_{k,b} \geq \lceil 1/\alpha\rceil$,
$P\bigl(Y \in \pset(X) \mid Y = k,\, b(X) = b\bigr) \geq (1 - \alpha) - \bar\delta_{\mathrm{TV}}^{(k,b)}$.
\end{theorem}

T3$'$ is the operative \emph{certificate} on Mendelian rather than a tight prediction: empirical $\bar\delta_{\mathrm{TV}}^{(k,b)} \leq 0.31$ on the worst cell yields a coverage lower bound of $0.59$, which is loose by construction (uniform over all distributions satisfying \ref{ass:A1prime} alone), while the observed per-chromosome coverage gap remains $\leq 0.073$ throughout. T3$'$ supplies the only uniform finite-sample lower bound on coverage when T3's exactness assumption (A2-cell) is empirically rejected. Our per-chromosome KS audit (App.~\ref{app:data}) shows the rejection rate is approximately $K$-invariant across $K_{\mathrm{eval}} \in \{2, 3, 5, 8, 10\}$ ($40$--$46\%$ on Mendelian), so T3$'$ supplies the certificate at every partition granularity.

\subsection{Optimal bin count and matching lower bound (T5.1, T5.2)}
\label{sec:theory:t5}

This is the principal theoretical contribution. T5.1 derives the optimal $K$ by formalising the bias-variance tradeoff and T5.2 proves a matching finite-sample lower bound \emph{within the equi-bin Mondrian-$K$ family} to establish that the $O(n^{-1/2})$ rate is tight on this class, dimension-free, and depends explicitly on $\pi_{\min}$ via the $\pi_{\min}^{-1/2}$ constant. The lower bound is not a distribution-free minimax bound over arbitrary CP procedures (other axes — e.g.\ self-calibrating CP \citep{vanderlaan2024sccp} — achieve different rates on different metrics).

Under \ref{ass:ASL}, the worst-cell coverage gap decomposes as
\begin{equation}
\label{eq:gap_decomp}
G(K) := \max_{k,b} |\mathrm{cov}_{k,b} - (1 - \alpha)| \leq \underbrace{\frac{L_F R}{K}}_{\text{stationarity bias}} + \underbrace{\frac{K}{\pi_{\min} n}}_{\text{finite-sample variance}},
\end{equation}
where $R = \shat_{\max} - \shat_{\min}$ and $\pi_{\min} = \min(\pi, 1 - \pi)$.

\begin{theorem}[T5.1 oracle bin count]
\label{thm:t5oracle}
Under \ref{ass:A1prime} and \ref{ass:ASL} with equiprobable $\shat$-bins,
$K^\star = \lfloor \sqrt{L_F R \pi_{\min} n}\rfloor$ minimises Eq.~\eqref{eq:gap_decomp}, with
$G(K^\star) \leq 2\sqrt{L_F R / (\pi_{\min} n)}$.
\end{theorem}

\begin{theorem}[T5.2 matching finite-sample lower bound on the equi-bin Mondrian-$K$ class]
\label{thm:t5lower}
Restrict attention to conformal predictors using equiprobable $\shat$-bin Mondrian partitions on the calibration fold (the class HCCP belongs to). There exists a distribution satisfying \ref{ass:A1prime} and \ref{ass:ASL} such that, for any $K$ within this class, $\min_K G(K) = \Omega\bigl(\sqrt{L_F R / (\pi_{\min} n)}\bigr)$. Hence the $O(n^{-1/2})$ rate of T5.1 is tight on this class, and the $\pi_{\min}^{-1/2}$ constant is not improvable without strengthening assumptions or moving outside the class.
\end{theorem}

\noindent\textbf{Proof sketch.} For T5.1: $g'(K) = -L_F R / K^2 + 1/(\pi_{\min} n) = 0$ gives $K^\star = \sqrt{L_F R \pi_{\min} n}$. For T5.2: a linear-shift family $F_{k,\sigma}(s) = \Phi(s - L_F \sigma)$ matches both bias and variance lower bounds. Full proofs in App.~\ref{app:proofs:t5}.

\noindent\textbf{Practical $\hat K$ via nested chrom-LOO.} Modal $\hat K(c_{\mathrm{outer}}) = 5$ Complex / $\{5, 8\}$ Mendelian. Plug-in LCLS $\hat L_F$ \citep{lcls2024lipschitz} gives asymptotic $K^\star = 22$ (Mendelian) / $50$ (Complex), above the modal nested-CV $\hat K$; the gap is the finite-sample fallback effect (App.~\ref{app:tables}). The synthetic $n$-sweep (App.~\ref{app:synthetic}) verifies the $O(n^{-1/2})$ rate: log-log slope $-0.50 \pm 0.05$ across feature dimensions $d \in \{8, 32, 128\}$, confirming dimension-free.

\subsection{Summary of guarantees}

\begin{table}[h]
\centering
\small
\caption{HCCP coverage guarantees at a glance. Auxiliary lemmas (T1, T2, T1$'$, T2$'$, T3-loc, T3.b, T4) live in App.~\ref{app:proofs}.}
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lll}
\toprule
Result & Assumptions & Guarantee \\
\midrule
T3 (Thm.~\ref{thm:t3}) & A1$'$ + A2-cell & $\mathrm{cov}_{k,b} \in [1-\alpha, 1-\alpha + 1/(n_{k,b}+1)]$ \\
T3$'$ (Thm.~\ref{thm:t3prime_main}) & A1$'$ only & $\mathrm{cov}_{k,b} \geq (1-\alpha) - \bar\delta_{\mathrm{TV}}^{(k,b)}$ \\
T5.1 (Thm.~\ref{thm:t5oracle}) & A1$'$ + \ref{ass:ASL} & $G(K^\star) \leq 2\sqrt{L_F R / (\pi_{\min} n)}$, $K^\star = O(\sqrt{n})$ \\
T5.2 (Thm.~\ref{thm:t5lower}) & equi-bin Mondrian-$K$ & $\min_K G(K) = \Omega(\sqrt{L_F R / (\pi_{\min} n)})$ \\
\bottomrule
\end{tabular}
\end{table}
